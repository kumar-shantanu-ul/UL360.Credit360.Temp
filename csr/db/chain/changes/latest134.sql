define version=134
@update_header

ALTER TABLE CHAIN.PURCHASED_COMPONENT
 ADD (MAPPED_DTM  DATE);

ALTER TABLE CHAIN.PURCHASED_COMPONENT
 ADD (MAPPED_BY_USER_SID  NUMBER(10));

ALTER TABLE chain.PURCHASED_COMPONENT ADD CONSTRAINT RefCHAIN_USER911
    FOREIGN KEY (APP_SID, MAPPED_BY_USER_SID)
    REFERENCES chain.CHAIN_USER(APP_SID, USER_SID)
;

@..\create_views
@..\purchased_component_pkg
@..\purchased_component_body

@update_tail