define version=37
@update_header

DROP INDEX UK_CAP_NAME_TYPE;
DROP INDEX UC_QUESTIONNAIRE_TYPE_CLASS;

ALTER TABLE CAPABILITY ADD CONSTRAINT UC_CAP_NAME_TYPE  UNIQUE (CAPABILITY_TYPE_ID, CAPABILITY_NAME, APP_SID);
ALTER TABLE QUESTIONNAIRE_TYPE ADD CONSTRAINT UC_QUESTIONNAIRE_TYPE_CLASS  UNIQUE (APP_SID, CLASS);

@update_tail
