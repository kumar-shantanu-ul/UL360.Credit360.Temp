-- Please update version.sql too -- this keeps clean builds in sync
define version=40
@update_header

-- 
-- TABLE: CUSTOMER_OPTIONS 
--

CREATE TABLE CUSTOMER_OPTIONS(
    APP_SID             NUMBER(10, 0)    DEFAULT SYS_CONTEXT('SECURITY','APP') NOT NULL,
    DEFAULT_COUNTRY     VARCHAR2(2),
    DEFAULT_CURRENCY    VARCHAR2(4),
    CONSTRAINT PK104 PRIMARY KEY (APP_SID)
)
;

ALTER TABLE CUSTOMER_OPTIONS ADD CONSTRAINT RefCOUNTRY161 
    FOREIGN KEY (DEFAULT_COUNTRY)
    REFERENCES COUNTRY(COUNTRY_CODE)
;

ALTER TABLE CUSTOMER_OPTIONS ADD CONSTRAINT RefCURRENCY162 
    FOREIGN KEY (DEFAULT_CURRENCY)
    REFERENCES CURRENCY(CURRENCY_CODE)
;


@update_tail
