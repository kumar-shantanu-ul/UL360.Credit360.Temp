-- Please update version.sql too -- this keeps clean builds in sync
define version=7
@update_header

ALTER TABLE GT_PDA_PROVENANCE_TYPE ADD (NATURAL NUMBER(1, 0) DEFAULT 0 NOT NULL);
ALTER TABLE GT_PDA_PROVENANCE_TYPE ADD (SCORE NUMBER(4, 2) DEFAULT 0 NOT NULL);
ALTER TABLE GT_PDA_ACCRED_TYPE ADD (SCORE NUMBER(4, 2) DEFAULT 0 NOT NULL);
	
@update_tail