-- Please update version.sql too -- this keeps clean builds in sync
define version=724
@update_header

ALTER TABLE csr.LOGISTICS_ERROR_LOG ADD LOCATION_TYPE_ID	NUMBER(10, 0)	NOT NULL;

ALTER TABLE csr.LOGISTICS_ERROR_LOG ADD CONSTRAINT RefLOCATION_TYPE2283 
    FOREIGN KEY (LOCATION_TYPE_ID)
    REFERENCES csr.LOCATION_TYPE(LOCATION_TYPE_ID)
;

@update_tail
