-- Please update version.sql too -- this keeps clean builds in sync
define version=1297
@update_header

ALTER TABLE CSR.STD_MEASURE_CONVERSION MODIFY A NUMBER(38,20);
ALTER TABLE CSR.STD_MEASURE_CONVERSION MODIFY B NUMBER(38,10);
ALTER TABLE CSR.STD_MEASURE_CONVERSION MODIFY C NUMBER(38,10);

ALTER TABLE CSR.CUSTOMER ADD APPLY_FACTORS_TO_CHILD_REGIONS     NUMBER(1, 0)      DEFAULT 1 NOT NULL;
ALTER TABLE CSR.CUSTOMER ADD CONSTRAINT CHK_CUS_APPLY_FCTRS_CHLDRN CHECK (APPLY_FACTORS_TO_CHILD_REGIONS IN (0,1));

UPDATE CSR.CUSTOMER SET APPLY_FACTORS_TO_CHILD_REGIONS = 0;

ALTER TABLE CSRIMP.CUSTOMER ADD APPLY_FACTORS_TO_CHILD_REGIONS     NUMBER(1, 0)      DEFAULT 1 NOT NULL;
ALTER TABLE CSRIMP.CUSTOMER ADD CONSTRAINT CHK_CUS_APPLY_FCTRS_CHLDRN CHECK (APPLY_FACTORS_TO_CHILD_REGIONS IN (0,1));

@..\csr_app_body
@..\csrimp\imp_body
@..\val_datasource_body
@..\stored_calc_datasource_body

@update_tail

