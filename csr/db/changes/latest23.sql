-- Please update version.sql too -- this keeps clean builds in sync
define version=23
@update_header

CREATE TABLE INSTANCE_DATAVIEW(
    INSTANCE_ID     NUMBER(10, 0)    NOT NULL,
    CONTEXT         NUMBER(10, 0)    NOT NULL,
    DATAVIEW_SID    NUMBER(10, 0)    NOT NULL,
    POS             NUMBER(10, 0)     DEFAULT 0 NOT NULL,
    CONSTRAINT PK144 PRIMARY KEY (INSTANCE_ID, CONTEXT, DATAVIEW_SID)
)
;


-- 
-- TABLE: INSTANCE_DATAVIEW 
--

ALTER TABLE INSTANCE_DATAVIEW ADD CONSTRAINT RefDATAVIEW237 
    FOREIGN KEY (DATAVIEW_SID)
    REFERENCES DATAVIEW(DATAVIEW_SID)
;

@update_tail
