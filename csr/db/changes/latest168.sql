-- Please update version.sql too -- this keeps clean builds in sync
define version=168
@update_header

CREATE INDEX IDX_AUDIT_LOG_APP_SID ON AUDIT_LOG(APP_SID)
;
CREATE UNIQUE INDEX IX_REGION_BACK_LINK ON REGION(APP_SID, LINK_TO_REGION_SID, REGION_SID)
TABLESPACE INDX
;
CREATE UNIQUE INDEX PK_ALT_TAG_GROUP ON TAG_GROUP(APP_SID, LOWER(NAME))
;

@update_tail
