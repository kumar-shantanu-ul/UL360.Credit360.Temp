-- Please update version.sql too -- this keeps clean builds in sync
define version=1876
@update_header

ALTER TABLE CSR.INITIATIVE_METRIC_ASSOC DROP CONSTRAINT PK_INITIATIVE_METRIC_ASSOC;

ALTER TABLE CSR.INITIATIVE_METRIC_ASSOC ADD (
    CONSTRAINT PK_INITIATIVE_METRIC_ASSOC PRIMARY KEY (APP_SID, PROJECT_SID, PROPOSED_METRIC_ID, MEASURED_METRIC_ID)
);

@update_tail
