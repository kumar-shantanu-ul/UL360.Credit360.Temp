define version=2689
@update_header

ALTER TABLE CSR.COURSE DROP CONSTRAINT FK_COURSE_REGION;

ALTER TABLE CSR.COURSE ADD CONSTRAINT FK_COURSE_REGION 
	FOREIGN KEY (APP_SID, REGION_SID) REFERENCES CSR.REGION (APP_SID, REGION_SID);

@update_tail
